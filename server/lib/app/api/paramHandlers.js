// import '../../models/'

export function characterid(req, res, next, id) {
    next();
}

export function templateid(req, res, next, id) {
    next();
}

export default {
    characterid,
    templateid
};
